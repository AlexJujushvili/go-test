package handler

import (
	"fmt"
	"net/http"
	"time"
)

// Handler ფუნქციას Vercel ავტომატურად გამოიძახებს
func Handler(w http.ResponseWriter, r *http.Request) {
	// განვსაზღვროთ კონტენტის ტიპი (UTF-8 საჭიროა ქართული ასოებისთვის)
	w.Header().Set("Content-Type", "text/plain; charset=utf-8")

	// მიმდინარე დრო
	currentTime := time.Now().Format("15:04:05")

	// ინფორმაცია, რომელსაც საიტი აჩვენებს
	fmt.Fprintf(w, "გამარჯობა! ეს Go სერვერი მუშაობს Vercel-ზე 🚀\n")
	fmt.Fprintf(w, "სერვერის მიმდინარე დრო: %s\n", currentTime)
	fmt.Fprintf(w, "თქვენი მოთხოვნის მისამართია: %s\n", r.URL.Path)
	fmt.Fprintf(w, "\nსტატუსი: ყველაფერი რიგზეა! ✅")
}
